## 2024-10-31 - [Cramped inputs on mobile]
**Learning:** Found two-column grid (`grid-cols-2`) in the monitor creation form that breaks responsiveness and creates cramped text inputs and select elements on mobile screens.
**Action:** Applied `grid-cols-1 sm:grid-cols-2` to allow them to stack vertically on small devices and display side-by-side on larger screens.
